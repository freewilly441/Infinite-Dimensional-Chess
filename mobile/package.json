{
  "name": "infinite-dimensional-chess-mobile",
  "version": "1.0.0",
  "description": "Capacitor mobile wrapper for Infinite Dimensional Chess",
  "scripts": {
    "sync-assets": "node sync-assets.js",
    "build": "npm run sync-assets && npx cap sync",
    "android": "npm run build && npx cap run android",
    "ios": "npm run build && npx cap run ios",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
